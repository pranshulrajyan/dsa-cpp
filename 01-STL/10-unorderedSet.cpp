#include <iostream>
using namespace std;
#include <unordered_set>

void explainunorderedSet()
{
    unordered_set<int> s;
    // not sorted but unique elements
    // lower bound and upper bound function does not work
    // All the other operations are the same as Set as listed in Set.cpp file
}